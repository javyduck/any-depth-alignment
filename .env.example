# Copy to .env and fill in. Never commit real keys.

# OpenAI — data generation (jailbroken-GPT corpus, benign responses) and the
# GPT-4o judge used by the adversarial-attack extraction (E3) and AutoDAN/PAIR/TAP.
OPENAI_API_KEY=

# Anthropic — the Claude Sonnet 4 row of the deep-prefill experiment (E2).
ANTHROPIC_API_KEY=

# HuggingFace — required for gated datasets/models (e.g. HEx-PHI, Gemma, Llama).
HF_TOKEN=
